---
// Elegant Editorial Footer
---

<footer class="footer">
  <div class="footer-top">
    <div class="footer-brand">
      <div class="brand-logo">
        <span class="logo-text">AHAM</span>
        <span class="logo-divider"></span>
        <span class="logo-sub">AUTOMOBILE</span>
      </div>
      <p class="brand-tagline">
        Exzellenz in jedem Detail.<br>
        Ihr Partner für außergewöhnliche Fahrzeuge.
      </p>
    </div>
    
    <div class="footer-grid">
      <div class="footer-col">
        <h4>Navigation</h4>
        <ul>
          <li><a href="/">Startseite</a></li>
          <li><a href="/fahrzeuge">Kollektion</a></li>
          <li><a href="/kontakt">Kontakt</a></li>
        </ul>
      </div>
      
      <div class="footer-col">
        <h4>Rechtliches</h4>
        <ul>
          <li><a href="/impressum">Impressum</a></li>
          <li><a href="/datenschutz">Datenschutz</a></li>
        </ul>
      </div>
      
      <div class="footer-col">
        <h4>Standort</h4>
        <address>
          Nuthedamm 17<br>
          14974 Ludwigsfelde<br>
          Deutschland
        </address>
      </div>
      
      <div class="footer-col">
        <h4>Kontakt</h4>
        <a href="tel:01708949479" class="footer-phone">0170 894 9479</a>
        <p class="footer-hours">
          Donnerstag 10–18 Uhr<br>
          & nach Vereinbarung
        </p>
      </div>
    </div>
  </div>
  
  <div class="footer-bottom">
    <div class="footer-line"></div>
    <div class="footer-bottom-content">
      <p>© {new Date().getFullYear()} AHAM Automobile. Alle Rechte vorbehalten.</p>
      <p class="footer-credit">Crafted with precision.</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--charcoal);
    padding: var(--space-xl) var(--space-lg) var(--space-md);
    position: relative;
  }
  
  .footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: var(--space-lg);
    right: var(--space-lg);
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }
  
  .footer-top {
    display: grid;
    grid-template-columns: 1.2fr 2fr;
    gap: var(--space-xl);
    margin-bottom: var(--space-lg);
  }
  
  /* Brand */
  .brand-logo {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-bottom: var(--space-md);
  }
  
  .brand-logo .logo-text {
    font-family: var(--font-display);
    font-size: 1.4rem;
    font-weight: 500;
    letter-spacing: 0.15em;
    color: var(--ivory);
  }
  
  .brand-logo .logo-divider {
    width: 1px;
    height: 18px;
    background: var(--gold);
  }
  
  .brand-logo .logo-sub {
    font-family: var(--font-headline);
    font-size: 0.55rem;
    font-weight: 500;
    letter-spacing: 0.4em;
    color: var(--silver);
  }
  
  .brand-tagline {
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-style: italic;
    color: var(--silver);
    line-height: 1.8;
  }
  
  /* Grid */
  .footer-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-md);
  }
  
  .footer-col h4 {
    font-family: var(--font-headline);
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: var(--space-sm);
  }
  
  .footer-col ul {
    list-style: none;
  }
  
  .footer-col li {
    margin-bottom: 10px;
  }
  
  .footer-col a {
    font-size: 0.9rem;
    color: var(--pearl);
    transition: color 0.3s ease;
  }
  
  .footer-col a:hover {
    color: var(--ivory);
  }
  
  .footer-col address {
    font-style: normal;
    font-size: 0.9rem;
    color: var(--pearl);
    line-height: 1.8;
  }
  
  .footer-phone {
    display: block;
    font-family: var(--font-display);
    font-size: 1.2rem;
    color: var(--ivory);
    margin-bottom: 10px;
  }
  
  .footer-hours {
    font-size: 0.85rem;
    color: var(--silver);
    line-height: 1.7;
  }
  
  /* Bottom */
  .footer-line {
    width: 100%;
    height: 1px;
    background: var(--slate);
    margin-bottom: var(--space-md);
  }
  
  .footer-bottom-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .footer-bottom-content p {
    font-size: 0.8rem;
    color: var(--silver);
  }
  
  .footer-credit {
    font-family: var(--font-display);
    font-style: italic;
  }
  
  @media (max-width: 1024px) {
    .footer {
      padding: var(--space-lg) var(--space-md) var(--space-md);
    }
    
    .footer-top {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }
    
    .footer-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (max-width: 600px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }
    
    .footer-bottom-content {
      flex-direction: column;
      gap: 10px;
      text-align: center;
    }
  }
</style>
